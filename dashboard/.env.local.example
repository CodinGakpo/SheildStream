# In local dev, the default (ws://localhost:8000/ws/dashboard, hardcoded as
# a fallback in app/page.tsx) is correct as-is — this file only matters once
# the gateway is deployed somewhere other than localhost:8000 (Week 11).
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/dashboard
